<!-- app/views/admin/user/index.phtml -->
<div class="crud-index">
    <div class="header">
        <h1>Users</h1>
        <a href="<?= $this->url->get('/admin/users/create') ?>" class="btn btn-primary">
            Create New User
        </a>
    </div>

    <!-- Search Form -->
    <form method="GET" class="search-form">
        <input type="text" name="search" placeholder="Search..." value="<?= $filters['search'] ?? '' ?>">
        <button type="submit">Search</button>
    </form>

    <!-- Data Table -->
    <table class="data-table">
        <thead>
            <tr>
                <th>ID</th>
                <th>Name</th>
                <th>Email</th>
                <th>Kuhnle ID</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
            <?php foreach ($data as $user): ?>
            <tr>
                <td><?= $user->user_id ?></td>
                <td><?= $this->escape($user->name) ?></td>
                <td><?= $this->escape($user->email) ?></td>
                <td><?= $user->kuhnle_id ?></td>
                <td class="actions">
                    <a href="<?= $this->url->get("/admin/users/{$user->user_id}") ?>">View</a>
                    <a href="<?= $this->url->get("/admin/users/{$user->user_id}/edit") ?>">Edit</a>
                    <form method="POST" action="<?= $this->url->get("/admin/users/{$user->user_id}") ?>" 
                          onsubmit="return confirm('Are you sure?')" style="display: inline;">
                        <input type="hidden" name="_method" value="DELETE">
                        <button type="submit" class="btn-link">Delete</button>
                    </form>
                </td>
            </tr>
            <?php endforeach; ?>
        </tbody>
    </table>

    <!-- Pagination -->
    <?php if ($data->hasPages()): ?>
    <div class="pagination">
        <!-- Pagination links would go here -->
    </div>
    <?php endif; ?>
</div>