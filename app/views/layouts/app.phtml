<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><?= htmlspecialchars($title ?? 'Our Framework') ?></title>
    <link rel="stylesheet" href="css/app.css">
</head>
<body>
    <div class="app-container">
        <!-- Top Identity Bar -->
        <header class="top-bar">
            <div class="brand">
                <h1><?= htmlspecialchars($brand ?? 'Our Framework') ?></h1>
            </div>
            <div class="user-menu">
                <span class="user-name"><?= htmlspecialchars($user_name ?? 'Guest') ?></span>
                <button class="menu-toggle" id="menuToggle">☰</button>
            </div>
        </header>

        <!-- Main Content Area -->
        <div class="main-container">
            <!-- Sidebar -->
            <aside class="sidebar" id="sidebar">
                <div class="sidebar-content">
                    <nav class="main-nav" id="mainNav">
                        <?php if (!empty($navigation)): ?>
                            <ul>
                                <?php foreach ($navigation as $item): ?>
                                    <li class="<?= $item['active'] ?? false ? 'active' : '' ?>">
                                        <a href="<?= htmlspecialchars($item['url']) ?>"><?= htmlspecialchars($item['text']) ?></a>
                                    </li>
                                <?php endforeach; ?>
                            </ul>
                        <?php else: ?>
                            <div class="loading">Loading menu...</div>
                        <?php endif; ?>
                    </nav>
                </div>
            </aside>

            <!-- Content Area -->
            <main class="content" id="content">
                <?php echo $this->yield('content'); ?>
            </main>
        </div>
    </div>
    <script src="js/app.js"></script>
</body>
</html>