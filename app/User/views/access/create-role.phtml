<?php 
/** @var \Core\Utils\Url $url */
$url = $this->getDI()->get('url'); 
?>
<h1>Create New Role</h1>

<div class="row">
    <div class="col-md-8">
        <div class="card">
            <div class="card-header">
                <h5>Role Information</h5>
            </div>
            <div class="card-body">
                <form method="post" action="<?= $url->get('user/access/create-role') ?>">
                    <div class="form-group">
                        <label for="name">Role Name (Code):</label>
                        <input type="text" id="name" name="name" class="form-control" 
                               placeholder="e.g., admin, manager, user" required maxlength="50">
                        <small class="form-text text-muted">Internal role identifier (lowercase, no spaces).</small>
                    </div>
                    
                    <div class="form-group">
                        <label for="description">Description:</label>
                        <textarea id="description" name="description" class="form-control" rows="3"
                                  placeholder="Describe what this role can do..."></textarea>
                        <small class="form-text text-muted">Optional description of the role's purpose.</small>
                    </div>
                    
                    <div class="form-group">
                        <button type="submit" class="btn btn-primary">Create Role</button>
                        <a href="<?= $url->get('user/access/roles') ?>" class="btn btn-secondary">Cancel</a>
                    </div>
                </form>
            </div>
        </div>
    </div>
    
    <div class="col-md-4">
        <div class="card">
            <div class="card-header">
                <h5>Guidelines</h5>
            </div>
            <div class="card-body">
                <ul>
                    <li><strong>Role Name:</strong> Use lowercase letters, numbers, and hyphens only</li>
                    <li>Examples: "admin", "manager", "user", "moderator"</li>
                    <li>Keep names short and descriptive</li>
                    <li>Roles define what users can access</li>
                    <li>Assign permissions to roles after creation</li>
                </ul>
            </div>
        </div>
    </div>
</div>