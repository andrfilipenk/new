<?php 
/** @var \Core\Utils\Url $url */
$url = $this->getDI()->get('url'); 
?>
<h1>Manage Permissions for: <?= htmlspecialchars($user->name) ?></h1>

<div class="row">
    <div class="col-md-6">
        <div class="card">
            <div class="card-header">
                <h5>User Information</h5>
            </div>
            <div class="card-body">
                <p><strong>ID:</strong> <?= htmlspecialchars($user->id) ?></p>
                <p><strong>Name:</strong> <?= htmlspecialchars($user->name) ?></p>
                <p><strong>Email:</strong> <?= htmlspecialchars($user->email) ?></p>
                
                <div class="mt-3">
                    <a href="<?= $url->get('user/access/user-permissions') ?>" class="btn btn-secondary">Back to User Permissions</a>
                </div>
            </div>
        </div>
    </div>
    
    <div class="col-md-6">
        <div class="card">
            <div class="card-header">
                <h5>Permission Management</h5>
            </div>
            <div class="card-body">
                <h6>Current Direct Permissions:</h6>
                <?php if (!empty($userPermissions)): ?>
                    <ul class="list-group mb-3">
                        <?php foreach ($userPermissions as $permission): ?>
                            <li class="list-group-item d-flex justify-content-between align-items-center">
                                <div>
                                    <strong><?= htmlspecialchars($permission->name) ?></strong>
                                    <br><small class="text-muted"><?= htmlspecialchars($permission->module) ?> / <?= htmlspecialchars($permission->controller) ?> / <?= htmlspecialchars($permission->action) ?></small>
                                    <br><span class="badge <?= $permission->granted ? 'badge-success' : 'badge-danger' ?>">
                                        <?= $permission->granted ? 'Granted' : 'Denied' ?>
                                    </span>
                                </div>
                                <form method="post" action="<?= $url->get('user/access/manage-user-permissions/' . $user->id) ?>" style="display: inline;">
                                    <input type="hidden" name="action" value="remove">
                                    <input type="hidden" name="permission_id" value="<?= $permission->id ?>">
                                    <button type="submit" class="btn btn-sm btn-outline-danger" 
                                            onclick="return confirm('Remove this permission from user?')">Remove</button>
                                </form>
                            </li>
                        <?php endforeach; ?>
                    </ul>
                <?php else: ?>
                    <p class="text-muted">User has no direct permissions assigned.</p>
                <?php endif; ?>
                
                <h6>Assign Permission:</h6>
                <form method="post" action="<?= $url->get('user/access/manage-user-permissions/' . $user->id) ?>">
                    <input type="hidden" name="action" value="assign">
                    
                    <div class="form-group">
                        <select name="permission_id" class="form-control" required>
                            <option value="">Select a permission...</option>
                            <?php foreach ($allPermissions as $permission): ?>
                                <option value="<?= $permission->id ?>">
                                    <?= htmlspecialchars($permission->name) ?> 
                                    (<?= htmlspecialchars($permission->module) ?>/<?= htmlspecialchars($permission->controller) ?>/<?= htmlspecialchars($permission->action) ?>)
                                </option>
                            <?php endforeach; ?>
                        </select>
                    </div>
                    
                    <div class="form-group">
                        <div class="form-check">
                            <input class="form-check-input" type="radio" name="granted" id="granted1" value="1" checked>
                            <label class="form-check-label" for="granted1">
                                Grant Permission
                            </label>
                        </div>
                        <div class="form-check">
                            <input class="form-check-input" type="radio" name="granted" id="granted0" value="0">
                            <label class="form-check-label" for="granted0">
                                Deny Permission (Override)
                            </label>
                        </div>
                    </div>
                    
                    <button type="submit" class="btn btn-warning">Assign Permission</button>
                </form>
            </div>
        </div>
    </div>
</div>