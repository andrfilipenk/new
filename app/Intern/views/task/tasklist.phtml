<?php 
/** @var \Core\Mvc\View $this */
$url = $this->getDI()->get('url');

/** @var \Module\Base\Model\Task[] $tasks */
function getFiltered($tasks, $status) {
    $result = [];
    foreach ($tasks as $task) {
        if ($task->status === $status) {
            $result[] = $task;
            continue;
        }
    }
    return $result;
}

?>
<h1>Task List</h1>
<a href="<?= $url->get('admin/task-create') ?>" class="btn btn-primary mb-2">Add New Task</a>
<div class="grid">
  <div class="task open">
    <h3>open</h3>
    <?php echo $this->items(getFiltered($tasks, 1), 'task/card-item'); ?>
  </div>
  <div class="task progress">
    <h3>progress</h3>
    <?php echo $this->items(getFiltered($tasks, 2), 'task/card-item'); ?>
  </div>
  <div class="task done">
    <h3>done</h3>
    <?php echo $this->items(getFiltered($tasks, 3), 'task/card-item'); ?>
  </div>
</div>

<table>
    <thead>
        <tr>
            <th>ID</th>
            <th>Title</th>
            <th>Creator</th>
            <th>Assigned</th>
            <th>Status</th>
            <th>Prio</th>
            <th>Dates</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
        <?php foreach ($tasks as $task): ?>
            <tr>
                <td><?= htmlspecialchars($task->id) ?></td>
                <td><?= htmlspecialchars($task->title) ?></td>
                <td><?= htmlspecialchars($task->creator ? $task->creator->name : 'Unknown') ?></td>
                <td><?= htmlspecialchars($task->assigned ? $task->assigned->name : 'Unknown') ?></td>
                <td><?= htmlspecialchars($task->status) ?></td>
                <td><?= htmlspecialchars($task->priority) ?></td>
                <td>
                    Created: <?= htmlspecialchars($task->created_at) ?><br>
                    Begin: <?= htmlspecialchars($task->begin_date) ?><br>
                    End: <?= htmlspecialchars($task->end_date) ?>
                </td>
                <td>
                    <a href="<?= $url->get('admin/task-edit/' . $task->id) ?>">E</a>
                    <a href="<?= $url->get('admin/task-delete/' . $task->id) ?>" onclick="return confirm('Delete this task?')">X</a>
                </td>
            </tr>
        <?php endforeach; ?>
    </tbody>
</table>