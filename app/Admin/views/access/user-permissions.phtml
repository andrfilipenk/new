<?php 
/** @var \Core\Utils\Url $url */
$url = $this->getDI()->get('url'); 
?>
<h1>User Permission Assignments</h1>

<div class="mb-3">
    <a href="<?= $url->get('admin/access') ?>" class="btn btn-secondary">Back to Overview</a>
    <a href="<?= $url->get('admin/access/permissions') ?>" class="btn btn-primary">Manage Permissions</a>
</div>

<div class="alert alert-info">
    <strong>Note:</strong> These are direct user permissions that bypass role assignments. 
    Users inherit permissions from their roles automatically.
</div>

<?php if (!empty($users)): ?>
    <table class="table table-striped">
        <thead>
            <tr>
                <th>ID</th>
                <th>User Name</th>
                <th>Email</th>
                <th>Direct Permissions</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
            <?php foreach ($users as $user): ?>
                <tr>
                    <td><?= htmlspecialchars($user->id) ?></td>
                    <td><?= htmlspecialchars($user->name) ?></td>
                    <td><?= htmlspecialchars($user->email) ?></td>
                    <td>
                        <span class="badge badge-secondary">View Details</span>
                    </td>
                    <td>
                        <a href="<?= $url->get('admin/access/manage-user-permissions/' . $user->id) ?>" 
                           class="btn btn-sm btn-warning">Manage Permissions</a>
                    </td>
                </tr>
            <?php endforeach; ?>
        </tbody>
    </table>
<?php else: ?>
    <div class="alert alert-info">
        <p>No users found.</p>
    </div>
<?php endif; ?>