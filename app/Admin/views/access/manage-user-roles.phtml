<?php 
/** @var \Core\Utils\Url $url */
$url = $this->getDI()->get('url'); 
?>
<h1>Manage Roles for: <?= htmlspecialchars($user->name) ?></h1>

<div class="row">
    <div class="col-md-6">
        <div class="card">
            <div class="card-header">
                <h5>User Information</h5>
            </div>
            <div class="card-body">
                <p><strong>ID:</strong> <?= htmlspecialchars($user->id) ?></p>
                <p><strong>Name:</strong> <?= htmlspecialchars($user->name) ?></p>
                <p><strong>Email:</strong> <?= htmlspecialchars($user->email) ?></p>
                
                <div class="mt-3">
                    <a href="<?= $url->get('admin/access/user-roles') ?>" class="btn btn-secondary">Back to User Roles</a>
                </div>
            </div>
        </div>
    </div>
    
    <div class="col-md-6">
        <div class="card">
            <div class="card-header">
                <h5>Role Management</h5>
            </div>
            <div class="card-body">
                <h6>Current Roles:</h6>
                <?php if (!empty($userRoles)): ?>
                    <ul class="list-group mb-3">
                        <?php foreach ($userRoles as $role): ?>
                            <li class="list-group-item d-flex justify-content-between align-items-center">
                                <div>
                                    <strong><?= htmlspecialchars($role->name) ?></strong>
                                    <?php if (!empty($role->description)): ?>
                                        <br><small class="text-muted"><?= htmlspecialchars($role->description) ?></small>
                                    <?php endif; ?>
                                </div>
                                <form method="post" action="<?= $url->get('admin/access/manage-user-roles/' . $user->id) ?>" style="display: inline;">
                                    <input type="hidden" name="action" value="remove">
                                    <input type="hidden" name="role_id" value="<?= $role->id ?>">
                                    <button type="submit" class="btn btn-sm btn-outline-danger" 
                                            onclick="return confirm('Remove this role from user?')">Remove</button>
                                </form>
                            </li>
                        <?php endforeach; ?>
                    </ul>
                <?php else: ?>
                    <p class="text-muted">User has no roles assigned.</p>
                <?php endif; ?>
                
                <h6>Assign Role:</h6>
                <form method="post" action="<?= $url->get('admin/access/manage-user-roles/' . $user->id) ?>" class="form-inline">
                    <input type="hidden" name="action" value="assign">
                    <select name="role_id" class="form-control mr-2" required>
                        <option value="">Select a role...</option>
                        <?php foreach ($allRoles as $role): ?>
                            <?php 
                            $isAssigned = false;
                            foreach ($userRoles as $userRole) {
                                if ($userRole->id == $role->id) {
                                    $isAssigned = true;
                                    break;
                                }
                            }
                            ?>
                            <?php if (!$isAssigned): ?>
                                <option value="<?= $role->id ?>"><?= htmlspecialchars($role->name) ?></option>
                            <?php endif; ?>
                        <?php endforeach; ?>
                    </select>
                    <button type="submit" class="btn btn-success">Assign Role</button>
                </form>
            </div>
        </div>
    </div>
</div>