<?php 
/** @var \Core\Utils\Url $url */
$url = $this->getDI()->get('url'); 
?>
<h1>Manage Holiday Request</h1>

<div class="row">
    <div class="col-md-6">
        <div class="card">
            <div class="card-header">
                <h5>Request Details</h5>
            </div>
            <div class="card-body">
                <p><strong>ID:</strong> <?= htmlspecialchars($request->id) ?></p>
                <p><strong>User ID:</strong> <?= htmlspecialchars($request->user_id) ?></p>
                <p><strong>Begin Date:</strong> <?= htmlspecialchars($request->begin_date) ?></p>
                <p><strong>End Date:</strong> <?= htmlspecialchars($request->end_date) ?></p>
                <p><strong>Duration:</strong> <?= $request->getDurationDays() ?> days</p>
                <p><strong>Current Status:</strong> 
                    <?php if ($request->isGranted()): ?>
                        <span class="badge badge-success">Granted</span>
                    <?php else: ?>
                        <span class="badge badge-warning">Pending</span>
                    <?php endif; ?>
                </p>
                <p><strong>Created:</strong> <?= htmlspecialchars($request->created_at ?? 'N/A') ?></p>
                
                <div class="mt-3">
                    <a href="<?= $url->get('admin/holidays') ?>" class="btn btn-secondary">Back to List</a>
                </div>
            </div>
        </div>
    </div>
    
    <div class="col-md-6">
        <div class="card">
            <div class="card-header">
                <h5>Grant/Deny Request</h5>
            </div>
            <div class="card-body">
                <form method="post" action="<?= $url->get('admin/holidays/manage/' . $request->id) ?>">
                    <div class="form-group">
                        <p>Choose an action for this holiday request:</p>
                        
                        <button type="submit" name="action" value="grant" 
                                class="btn btn-success mr-2"
                                <?= $request->isGranted() ? 'disabled' : '' ?>>
                            <?= $request->isGranted() ? 'Already Granted' : 'Grant Request' ?>
                        </button>
                        
                        <button type="submit" name="action" value="deny" 
                                class="btn btn-warning"
                                onclick="return confirm('Deny this holiday request?')">
                            Deny Request
                        </button>
                    </div>
                </form>
                
                <hr>
                
                <div class="mt-3">
                    <h6>Notes:</h6>
                    <ul>
                        <li>Granting the request will mark it as approved</li>
                        <li>Denying will set the status back to pending/denied</li>
                        <li>Users can see the status of their requests</li>
                    </ul>
                </div>
            </div>
        </div>
    </div>
</div>